# Runtime dependencies for the SIH26162 Thermal Intelligence FastAPI backend.
# Mirrors apps/api/pyproject.toml[project].dependencies. Render's Python
# buildpack installs from this file (no `-e .` editable install), so the
# `app` package is served from the repo root at runtime via uvicorn app.main:app.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29.0
alembic>=1.13.0
geoalchemy2>=0.14.0
shapely>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
